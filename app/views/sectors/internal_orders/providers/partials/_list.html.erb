<% if @internal_orders.present? %>
    <div class="card fixed-panel-list">
      <table class="table table-hover table-striped table-sm">
        <thead>
          <tr>
            <%= th_label name: 'Código', form: 'internal-filter', sort_field: 'remit_code', icon_type: 'alpha' %>
            <%= th_label name: 'Solicitante', form: 'internal-filter', sort_field: 'sectors.name', icon_type: 'alpha' %>
            <%= th_label name: 'Origen' %>
            <%= th_label name: 'Estado' %>
            <th>
              <%= fa_icon "cubes" %>
            </th>
            <th>
              <%= fa_icon "exchange-alt" %>
            </th>
            <th>
              <%= fa_icon "comments" %>
            </th>
            <th>
               <%= fa_icon "calendar" %>
              Solicitado
            </th>
            <th>
               <%= fa_icon "calendar" %>
              Entregado
            </th>
            <th class="text-center">Acciones</th>
          </tr>
        </thead>
        <tbody id="internal_orders">
          <%= render partial: "sectors/internal_orders/providers/partials/provider", collection: @internal_orders, as: :internal_order %>
        </tbody>
      </table>
    </div>
  
<% else %>
  <%= render(
    partial: 'shared/not_found_filter',
    locals: { object_name: 'entregas' }
  ) %>
<% end %>
