
<div class="modal fade" id="rollback-order" tabindex="-1" aria-labelledby="rollback-orderLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content panel-warning">
      <div class="modal-header panel-heading bg-danger">
        <h5 class="m-0 d-flex align-items-center text-white">
          <%= fa_icon "undo" %> 
          <div class="ml-2 title">Retornar recibo <%= @receipt.remit_code %></div>
        </h5>
        <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="mb-2 body-title">Se devolveran las cantidades de stock a su estado anterior</div>
        <p>
          Está seguro de que desea retornar?
        </p>            
      </div>
      <div class="modal-footer text-right">
        <a href="#" data-dismiss="modal" class="btn btn-secondary">Cancelar</a>
        <%= link_to rollback_order_receipt_path(@receipt), method: :patch,
          class: 'btn btn-danger',
          data: { 
            "disable-with": "Retornando...<i class='fa fa-spinner fa-spin'></i>"
          } do %>
          Continuar
        <% end %>
      </div>
    </div>
  </div>
</div>