$("input[name='patient_ids']").val("<%= @patient_ids %>");
$("button#sp-<%= @patient_id %>").remove();
<% sector_patients = escape_javascript(
  render(partial: 'reports/partials/sector_patients')
) %>

$("#patients-list").html("<%= sector_patients %>");

$("#loader-container").css({'display': 'none'});