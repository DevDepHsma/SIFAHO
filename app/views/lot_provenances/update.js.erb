<%= render 'shared/flash_message' %>

$('#dialog').modal('toggle');

<% js = escape_javascript(
  render(partial: 'lot_provenances/list', locals: { lot_provenances: @lot_provenances })
) %>

$("#lots_provenance").html("<%= js %>");
