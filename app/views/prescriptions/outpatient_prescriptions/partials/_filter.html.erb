<%= simple_form_for  outpatient_prescriptions_path, remote: true, html: { method: 'GET', class: 'custom-filters-v1', id: 'outpatient-prescriptions-filter'} do |f| %>
  <div class="row">
    <%# <div class="col d-flex justify-content-end align-items-center">
      <%= link_to outpatient_prescriptions_path(reset: true), class: 'btn btn-secondary ml-2',
        title: 'Reiniciar', data: { toggle: 'tooltip', placement: 'top' }, 
        'data-disable-with' => "<i class='fa fa-redo fa-spin'></i>".html_safe do 
        <%= fa_icon 'redo'
      <% end 
    </div> %>
    <div class="col custom-col-input">
      <div class="form-group">
        <input class="form-control" 
               autocomplete="off" 
               type="text" 
               name="filter[code]" 
               id="filter_code" 
               placeholder="Código"
               value="<%= get_value_from_filter(:code) %>">
        <label>Código</label>
      </div>
    </div>
    <div class="col custom-col-input">
      <div class="form-group">
        <input class="form-control" 
               autocomplete="off" 
               type="text" 
               name="filter[professional_full_name]" 
               id="filter_professional_full_name" 
               placeholder="Médico: Apellido / Nombre"
               value="<%= get_value_from_filter(:professional_full_name) %>">
        <label>Médico: Apellido / Nombre</label>
      </div>
    </div>
    <div class="col custom-col-input">
      <div class="form-group">
        <input class="form-control" 
               autocomplete="off" 
               type="text" 
               name="filter[patient_full_name]" 
               id="filter_patient_full_name" 
               placeholder="Paciente: Apellido / Nombre / DNI"
               value="<%= get_value_from_filter(:patient_full_name) %>">
        <label>Paciente: Apellido / Nombre / DNI</label>
      </div>
    </div>
    
    <div class="col custom-col-input">
      <div class="form-group">
        <input class="form-control datepicker" 
               autocomplete="off" 
               type="text" 
               name="filter[date_prescribed_since]" 
               id="filter_date_prescribed_since" 
               placeholder="Prescripta desde: dd/mm/aaaa"
               value="<%= get_value_from_filter(:date_prescribed_since) %>">
        <label>Prescripta desde: dd/mm/aaaa</label>
      </div>
    </div>
    <div class="col custom-col-input">
      <div class="form-group">
        <input class="form-control datepicker" 
               autocomplete="off" 
               type="text" 
               name="filter[date_prescribed_to]" 
               id="filter_date_prescribed_to" 
               placeholder="Prescripta hasta: dd/mm/aaaa"
               value="<%= get_value_from_filter(:date_prescribed_to) %>">
        <label>Prescripta hasta: dd/mm/aaaa</label>
      </div>
    </div>
    <%# <div class="col"> %>
      <%# <label>Profesional</label> %>
      <%# give the search field the 'filterrific-periodically-observed' class for live updates %>
      <%#= f.text_field(
        :search_by_professional,
        class: 'filterrific-periodically-observed form-control input-sm',
        placeholder: 'Nombre o apellido',
        autocomplete: 'off',
      ) %>
    <%# </div> %>
    <%# <div class="col"> %>
      <%# <label>Paciente</label> %>
      <%# give the search field the 'filterrific-periodically-observed' class for live updates %>
      <%#= f.text_field(
        :search_by_patient,
        class: 'filterrific-periodically-observed form-control input-sm',
        placeholder: 'Nombre/Apellido/DNI',
        autocomplete: 'off',
      ) %>
    <%# </div> %>
    <%# <div class="col"> %>
      <%# <label>Recetado desde</label> %>
      <%#= f.text_field(:date_prescribed_since, autocomplete: 'off',
        class: 'datepicker form-control filterrific-periodically-observed input-sm',
        placeholder: "Una fecha", data: { date_format: 'DD/MM/YYYY'}) %>
    <%# </div> %>
    <%# <div class="col"> %>
      <%# <label>Estado</label> %>
        <%#= f.select(:for_statuses,
          @filterrific.select_options[:for_statuses].map {
            |status| [status.first, status.second, data: {'content': "<span class='badge badge-"+status.third+"'>"+status.first+"</span>"}]
          },{ include_blank: false, include_hidden: false },
          {
            class: "selectpicker-md show-tick custom-select-pick", 
            multiple: true,
            allow_blank: false,
            title: 'Estado',
            "data-width"=>"100%"
          }
        ) %>
    <%# </div> %>
    <%# <div class="col"> %>
      <%# <label>Ordernar por</label> %>
      <%#= f.select(:sorted_by,
        @filterrific.select_options[:sorted_by].map {
          |status| [status.first, status.second]
        },{},
        { class: "selectpicker-md show-tick custom-select-pick", "data-width"=>"100%"}) %>
    <%# </div> %>
  </div>
<% end %>
<button class="btn btn-primary" type="submit" form="outpatient-prescriptions-filter">
  Buscar
</button>